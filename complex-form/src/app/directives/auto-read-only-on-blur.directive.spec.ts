import { AutoReadOnlyOnBlurDirective } from './auto-read-only-on-blur.directive';

describe('AutoReadOnlyOnBlurDirective', () => {
  it('should create an instance', () => {
    const directive = new AutoReadOnlyOnBlurDirective();
    expect(directive).toBeTruthy();
  });
});
