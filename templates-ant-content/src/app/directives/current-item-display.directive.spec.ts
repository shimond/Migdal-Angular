import { CurrentItemDisplayDirective } from './current-item-display.directive';

describe('CurrentItemDisplayDirective', () => {
  it('should create an instance', () => {
    const directive = new CurrentItemDisplayDirective();
    expect(directive).toBeTruthy();
  });
});
